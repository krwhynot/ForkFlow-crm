import Demo from './Demo';

/**
 * ForkFlow Food Broker CRM Application Entry Point - Demo Mode
 *
 * This version uses fake data and bypasses authentication for easy testing.
 * Switch back to the full CRM component when ready for production.
 */
const App = () => <Demo />;

export default App;
